import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#000',
  },
  controller: {
    marginTop: 100,
    flexDirection: 'row',
  },
});

export default styles;
